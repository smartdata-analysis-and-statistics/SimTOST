% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{.power_count_serial}
\alias{.power_count_serial}
\title{Estimate power for count-rate equivalence}
\usage{
.power_count_serial(
  n_per_arm,
  rate_test,
  rate_reference,
  exposure = 1,
  margin_lower = 0.8,
  margin_upper = 1.25,
  model = c("poisson", "negative-binomial"),
  dispersion = 0.1,
  alpha = 0.05,
  nsim = 5000,
  seed = NULL,
  design = c("parallel", "2x2"),
  k = NULL,
  endpoint_corr = NULL,
  type_y = NULL,
  adjust = c("none", "bonferroni", "sidak", "t", "pc", "partial-conjunction",
    "partial_conjunction", "sequential"),
  sigmaB = 0,
  Eper = c(0, 0),
  Eco = c(0, 0),
  dropout = c(0, 0),
  type_y_active = FALSE
)
}
\arguments{
\item{n_per_arm}{Subjects per arm.}

\item{rate_test}{Event rate in the test arm.}

\item{rate_reference}{Event rate in the reference arm.}

\item{exposure}{Exposure per subject; a scalar or one value per endpoint.}

\item{margin_lower}{Lower rate-ratio margin; a scalar or one value per endpoint.}

\item{margin_upper}{Upper rate-ratio margin; a scalar or one value per endpoint.}

\item{model}{Count model: \code{"poisson"} or \code{"negative-binomial"}.}

\item{dispersion}{Positive negative-binomial dispersion parameter.}

\item{alpha}{One-sided significance level.}

\item{nsim}{Number of simulations.}

\item{seed}{Optional random seed.}

\item{design}{Trial design: \code{"parallel"} or \code{"2x2"}. For \code{"2x2"},
\code{n_per_arm} is interpreted as subjects per sequence.}

\item{k}{Number of endpoints that must demonstrate equivalence. Defaults to
all supplied endpoints.}

\item{endpoint_corr}{Endpoint correlation matrix used by the Gaussian
copula for multi-endpoint count simulations. The default is independence.}

\item{type_y}{Numeric endpoint hierarchy used with \code{adjust = "seq"}: \code{1}
for primary/co-primary endpoints and \code{2} for secondary endpoints. Named
vectors are recommended when endpoint names are available.}

\item{adjust}{Multiplicity adjustment for endpoint-wise one-sided alpha:
\code{"none"}, \code{"bonferroni"}, \code{"sidak"}, \code{"t"}, or \code{"seq"}/\code{"sequential"}.
The \code{"t"} option uses Mielke's strong k-out-of-m calibration
\code{alpha / (m - k + 1)}; legacy partial-conjunction labels are accepted.
Sequential testing applies the same primary-gate/secondary-family rule as
the continuous kernels.
When \code{k} equals the number of supplied endpoints, endpoint-wise adjustment
is not necessary for the all-endpoints-required intersection-union decision;
the requested method is retained but a warning is issued.}

\item{sigmaB}{Between-subject standard deviation on the log-rate scale for
the count \verb{2x2} design.}

\item{Eper}{Numeric vector of length 2 containing period effects on the
log-rate scale.}

\item{Eco}{Numeric vector of length 2 containing carry-over effects on the
log-rate scale, ordered as reference carry-over and treatment carry-over.}

\item{dropout}{Numeric vector of length 2 containing dropout proportions
for the two crossover sequences.}
}
\value{
An object of class \code{countpower} containing estimated power and its
binomial confidence interval.
}
\description{
Estimate power for count-rate equivalence
}
\details{
For \code{design = "2x2"}, complete participants contribute one count
under each treatment. The kernel analyzes within-participant log-rate
contrasts, averages the two sequence-specific estimates to remove period
effects, and applies the carry-over correction implied by
\code{Eco = c(reference_carryover, treatment_carryover)}. \code{exposure} is used as
the log-rate offset. \code{sigmaB} is the standard deviation of a subject
random intercept used in the count-generating model; it cancels from the
within-participant treatment contrast. The standard error is estimated from
the empirical variance of the subject-level contrasts. Participants who
drop out before completing both periods do not contribute to this paired
analysis.
}
\examples{
SimTOST:::power_count(40, 0.20, 0.20, nsim = 100, seed = 1)
}
