% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution_plots.R
\name{plot_distribution}
\alias{plot_distribution}
\title{Plot distributions of retained simulated observations}
\usage{
plot_distribution(x, estimand = "mu", arms = NULL, endpoints = NULL, ...)
}
\arguments{
\item{x}{A result returned by \code{sampleSize()} or \code{simPower()} with
\code{keep_sim_data = TRUE}.}

\item{estimand}{Quantity to display: \code{"mu"} (or \code{"mean"}), \code{"sigma"},
\code{"dispersion"}, \code{"rate"}, \code{"t_value"},
\code{"DOM"}, \code{"ROM"}, \code{"RR"}, or \code{"correlation"}. \code{"mean"} and \code{"sigma"}
display arm-specific simulation parameters; \code{"DOM"} and \code{"ROM"} display
continuous comparison estimates; \code{"RR"} displays count rate ratios; and
\code{"correlation"} displays endpoint correlations.}

\item{arms}{Optional character vector of arm names. For \code{mean} and \code{sigma},
these are the arms to facet. For comparison estimands, these select
comparator panels containing at least one supplied arm. If \code{NULL}, all
are shown.}

\item{endpoints}{Optional character vector of endpoint names. If \code{NULL},
all endpoints are displayed.}

\item{...}{Optional plotting controls, including \code{type}, \code{show_reference},
\code{n_trials}, \code{seed}, and \code{max_points}. Legacy selector names are accepted
for backward compatibility.}

\item{type}{Plot type: \code{"density"}, \code{"histogram"}, \code{"ecdf"}, or \code{"qq"}.}

\item{show_reference}{Logical. If \code{TRUE}, overlay the user-specified
outcome distribution or the true estimand value. Defaults to \code{TRUE}.}

\item{n_trials}{Optional number of randomly selected simulated trials to
display. This is useful for illustrating the empirical distribution
without plotting every retained observation.}

\item{seed}{Seed used when selecting \code{n_trials}.}

\item{max_points}{Maximum number of observations plotted after filtering.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates density, histogram, ECDF, or Q--Q plots for a selected
trial-level quantity. The \code{estimand} argument selects an arm parameter, a
reconstructed TOST statistic, a continuous comparison, a count rate ratio,
or endpoint correlations.
}
\details{
Every comparator follows the package
convention \code{c(test, reference)}. For \code{t_value}, the lower
and upper TOST statistics are reconstructed from the retained outcomes
using the same continuous parallel formulas as the test. The dashed lines
are the corresponding one-sided critical values. The plotted DOM is
\code{test - reference}
and the plotted ROM is \code{test / reference}; reversing the comparator reverses
the displayed estimand. The plotted RR is \code{rate_test / rate_reference}.
}
