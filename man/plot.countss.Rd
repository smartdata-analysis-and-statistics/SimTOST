% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simpower_methods.R
\name{plot.countss}
\alias{plot.countss}
\title{Plot count-outcome sample-size results}
\usage{
\method{plot}{countss}(x, target_power = 0.8, display = "all", all = TRUE, endpoint = NULL, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=sampleSize]{sampleSize()}}. Count-specific dispatch is
retained through the secondary compatibility class.}

\item{target_power}{Target power shown as a horizontal reference line. The
default is 0.80, unless the object stores a planning target, in which
case that target is used when this argument is omitted. An explicit value
always overrides the stored target.}

\item{display}{Character vector of comparator panels to display. Use
\code{"all"} (the default) to display every comparator and, for joint count
analyses, the all-comparisons panel.}

\item{all}{Logical. If \code{TRUE} (the default), display only the aggregate
\verb{All comparators} result without a comparator facet. If \code{FALSE}, display
the comparator panels selected by \code{display}.}

\item{endpoint}{Endpoint names to display. By default, \code{"Total"} is used
when \code{all = TRUE}, while all retained endpoints are used when \code{all = FALSE}.
Use \code{"all"} to show all retained endpoints explicitly.}

\item{...}{Unused additional arguments.}
}
\value{
A \code{ggplot} object showing the simulated power curve over the
evaluated candidate sample sizes, with the selected sample size highlighted.
Confidence intervals are shown as clearly visible vertical lines through
each point.
If an older \code{countss} object has no search history, the plot falls back to
the selected-sample-size point.
}
\description{
Plot count-outcome sample-size results
}
