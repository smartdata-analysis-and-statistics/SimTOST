% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_joint.R
\name{.power_count_joint_serial}
\alias{.power_count_joint_serial}
\title{Estimate joint power for correlated count endpoints and multiple comparisons}
\usage{
.power_count_joint_serial(
  n_per_arm,
  rates,
  comparisons,
  exposure = 1,
  margin_lower = 0.8,
  margin_upper = 1.25,
  model = c("poisson", "negative-binomial"),
  dispersion = 0.1,
  alpha = 0.05,
  endpoint_corr = NULL,
  k = NULL,
  type_y = NULL,
  adjust = c("none", "bonferroni", "sidak", "t", "pc", "partial-conjunction",
    "partial_conjunction", "sequential"),
  nsim = 5000,
  seed = NULL,
  design = c("parallel"),
  list_margin_lower = NULL,
  list_margin_upper = NULL,
  type_y_active = FALSE
)
}
\arguments{
\item{n_per_arm}{Subjects in each arm. This joint implementation supports
parallel-group designs.}

\item{rates}{Named list of equal-length endpoint-rate vectors, one per arm.}

\item{comparisons}{Named list of length-two character vectors. The first arm
is the test arm and the second is the reference arm.}

\item{exposure}{Exposure per subject, scalar or one value per endpoint,
or a named list with one scalar/vector per arm.}

\item{margin_lower}{Lower rate-ratio equivalence margin.}

\item{margin_upper}{Upper rate-ratio equivalence margin.}

\item{model}{Count model: \code{"poisson"} or \code{"negative-binomial"}.}

\item{dispersion}{Positive negative-binomial dispersion parameter, scalar
or a named list with one scalar/vector per arm.}

\item{alpha}{One-sided significance level, scalar or one value per endpoint.}

\item{endpoint_corr}{Positive-definite latent Gaussian correlation matrix
across endpoints. The default is independence.}

\item{k}{Number of endpoints that must pass within every comparison.}

\item{type_y}{Numeric endpoint hierarchy used with \code{adjust = "seq"}: \code{1}
for primary/co-primary endpoints and \code{2} for secondary endpoints.}

\item{adjust}{Multiplicity adjustment within each comparison's selected
endpoint family: \code{"none"}, \code{"bonferroni"}, \code{"sidak"}, \code{"t"}, or
\code{"seq"}/\code{"sequential"}. The \code{"t"} option uses Mielke's strong
k-out-of-m calibration \code{alpha / (m - k + 1)}; legacy partial-conjunction
labels are accepted. Sequential testing uses the primary gate and
secondary-family rule used by the continuous kernels.
When all supplied endpoints are required (\code{k} equals the endpoint count),
endpoint-wise adjustment is not necessary for the intersection-union
decision; a requested adjustment remains available with a warning.}

\item{nsim}{Number of simulated trials.}

\item{seed}{Optional random seed.}

\item{design}{Joint multi-arm design; currently only \code{"parallel"} is
supported because multiple reference arms do not define a single standard
2x2 crossover design.}

\item{list_margin_lower}{Optional named list of lower margins, one vector
per comparison. Each vector is scalar or has one value per endpoint.}

\item{list_margin_upper}{Optional named list of upper margins, one vector
per comparison. Each vector is scalar or has one value per endpoint.}
}
\value{
An object of class \code{countpower} containing joint power and a
binomial confidence interval.
}
\description{
Each simulated trial contains all arms and endpoints. Endpoint counts are
generated with their requested marginal Poisson or negative-binomial
distributions and a Gaussian-copula dependence structure. Every comparison
must pass at least \code{k} endpoints for the trial to count as a success.
}
\examples{
rates <- list(TEST = c(.20, .20), REF = c(.20, .20), ALT = c(.20, .20))
SimTOST:::power_count_joint(100, rates, list(REF = c("TEST", "REF"),
                     ALT = c("TEST", "ALT")), nsim = 100, seed = 1)
}
