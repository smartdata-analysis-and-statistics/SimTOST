% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{sampleSize_count}
\alias{sampleSize_count}
\title{Estimate sample size for count-rate equivalence}
\usage{
sampleSize_count(
  power = 0.8,
  rate_test,
  rate_reference,
  exposure = 1,
  margin_lower = 0.8,
  margin_upper = 1.25,
  model = c("poisson", "negative-binomial"),
  dispersion = 0.1,
  alpha = 0.05,
  nsim = 5000,
  seed = NULL,
  lower = 2,
  upper = 500,
  design = c("parallel", "2x2"),
  k = NULL,
  endpoint_corr = NULL,
  type_y = NULL,
  adjust = c("none", "bonferroni", "sidak", "t", "pc", "partial-conjunction",
    "partial_conjunction", "sequential"),
  sigmaB = 0,
  Eper = c(0, 0),
  Eco = c(0, 0),
  dropout = c(0, 0),
  optimization_method = c("fast", "step-by-step"),
  step.power = 6,
  step.up = TRUE,
  pos.side = FALSE,
  maxiter = 1000,
  ncores = 1,
  .warn_redundant_bon = TRUE
)
}
\arguments{
\item{power}{Target power.}

\item{rate_test}{Event rate in the test arm.}

\item{rate_reference}{Event rate in the reference arm.}

\item{exposure}{Exposure per subject; a scalar or one value per endpoint.}

\item{margin_lower}{Lower rate-ratio equivalence margin.}

\item{margin_upper}{Upper rate-ratio equivalence margin.}

\item{model}{Count model: \code{"poisson"} or \code{"negative-binomial"}.}

\item{dispersion}{Positive negative-binomial dispersion parameter.}

\item{alpha}{One-sided significance level.}

\item{nsim}{Number of simulated trials.}

\item{seed}{Optional random seed.}

\item{lower}{Minimum subjects per arm.}

\item{upper}{Maximum subjects per arm.}

\item{design}{Trial design: \code{"parallel"} or \code{"2x2"}. In a crossover
design, the returned sample size is per sequence.}

\item{k}{Number of endpoints that must demonstrate equivalence. Defaults to
all supplied endpoints.}

\item{endpoint_corr}{Endpoint correlation matrix used by the Gaussian
copula for multi-endpoint count simulations. The default is independence.}

\item{type_y}{Numeric endpoint hierarchy used with \code{adjust = "seq"}: \code{1}
for primary/co-primary endpoints and \code{2} for secondary endpoints.}

\item{adjust}{Multiplicity adjustment for endpoint-wise one-sided alpha:
\code{"none"}, \code{"bonferroni"}, \code{"sidak"}, \code{"t"}, or \code{"seq"}/\code{"sequential"}.
The \code{"t"} option uses Mielke's strong k-out-of-m calibration
\code{alpha / (m - k + 1)}; legacy partial-conjunction labels are accepted.}

\item{sigmaB}{Between-subject standard deviation for the count 2x2 design.}

\item{Eper}{Period effects for the count 2x2 design.}

\item{Eco}{Carry-over effects for the count 2x2 design.}

\item{dropout}{Dropout proportions for the count 2x2 design.}

\item{optimization_method}{Search method. \code{"fast"} brackets the power
crossing and uses integer bisection; \code{"step-by-step"} evaluates every
candidate sample size.}

\item{step.power}{Initial power-of-two jump used by the fast search.}

\item{step.up}{Direction of the initial bracketing search.}

\item{pos.side}{Retained for compatibility with \code{sampleSize()}; count
searches return the smallest candidate reaching the target.}

\item{maxiter}{Maximum number of power evaluations.}

\item{ncores}{Number of worker processes used for count simulations.
Set to 1 for serial execution. Parallel execution splits \code{nsim} into
reproducible independent chunks and combines the resulting successes.}

\item{.warn_redundant_bon}{Logical. If \code{TRUE}, warn about redundant or
uncalibrated multiplicity configurations.}
}
\value{
An object of class \code{countss} containing the selected sample size,
achieved power, confidence interval, input parameters, and the search
history in \code{table.iter} and \code{table.test}. For count outcomes, \code{table.iter}
has one row per evaluated candidate sample size. \code{table.test} contains
complete-trial, comparator, and endpoint decision indicators for each
simulated trial and candidate. The count kernel returns aggregate decision
counts rather than raw endpoint-level test statistics, so component columns
preserve the simulated marginal success counts.
}
\description{
Searches for the smallest number of subjects per arm whose
simulated power reaches the target for a rate-ratio equivalence test.
}
\examples{
SimTOST:::sampleSize_count(0.80, 0.20, 0.20, lower = 100, upper = 2000,
                 nsim = 100, seed = 1)
}
