% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type1Error.R
\name{type1Error}
\alias{type1Error}
\title{Empirical Type I Error at the Least-Favorable Null}
\usage{
type1Error(
  null = c("lower", "upper", "both"),
  x = NULL,
  comparator = NULL,
  endpoint = NULL,
  joint = FALSE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{null}{Boundary to evaluate: \code{"lower"}, \code{"upper"}, or \code{"both"}.}

\item{x}{Optional existing \code{simss} or \code{simpower} object. When supplied, its
design and outcome parameters are reused; arguments in \code{...} override
stored settings such as \code{n} or \code{nsim}.}

\item{comparator}{Optional comparator name identifying the boundary
scenario when \code{joint = FALSE}. It must be one of the names in
\code{list_comparator}; by default, the first comparator is used. It is
ignored when \code{joint = TRUE}, because joint mode evaluates all comparators.}

\item{endpoint}{Optional endpoint identifying the boundary component when
\code{joint = FALSE}. It is available for the all-endpoints-required case
(\code{k} equal to the number of endpoints), where one endpoint is placed on
the boundary. For \code{k} smaller than the number of endpoints, use
\code{joint = TRUE} so that all composite boundary configurations are evaluated.}

\item{joint}{Logical. If \code{TRUE}, evaluate every comparator and every
endpoint partial-null configuration and return the joint Type I-error
result for each complete-trial decision. For a comparator with \code{m}
endpoints and an at-least-\code{k} rule, all boundary counts from
\code{m - k + 1} through \code{m} are evaluated. Defaults to \code{FALSE} for backward
compatibility.}

\item{conf.level}{Confidence level for the simultaneous one-sided Monte
Carlo upper bound across the evaluated joint scenarios. Defaults to
\code{0.95}.}

\item{...}{Arguments passed to \code{\link[=simPower]{simPower()}}. The call must include the
outcome parameters, comparator definitions, and equivalence margins.}
}
\value{
For \code{null = "lower"} or \code{"upper"}, an object of class
\code{type1error} containing the empirical Type I error in \code{type1_error} and
its Monte Carlo interval. For \code{null = "both"}, a named list containing
the lower- and upper-bound results. With \code{joint = TRUE}, a
\code{type1error_joint} object containing one joint result for every valid
comparator, endpoint-subset, boundary-direction combination is returned.
}
\description{
Generates a boundary-null configuration and estimates its empirical
rejection probability using \code{\link[=simPower]{simPower()}}. The comparator convention is
\code{c(test, reference)}. For normal ROM and count RR, a lower-bound null
sets \code{mean(test) / mean(reference) = L}. For log-normal ROM, the boundary
is imposed on the log-analysis scale used by the test; this coincides with
the arithmetic mean ratio when the two arms have the same coefficient of
variation.
}
\details{
Comparators use the convention \code{c(test, reference)}. For ROM,
the tested estimand is \code{test / reference}; for DOM it is
\code{test - reference}. With log-normal ROM, the test is performed after
converting the supplied original-scale means and variances to the
log-analysis scale, so the boundary scenario is calibrated on that
scale. For count rate ratios, the analogous midpoint is \code{sqrt(L * U)} on
the rate-ratio scale because the test is performed on the log-rate-ratio
scale. Absolute rates and dispersion remain nuisance parameters, so this
midpoint should be supplemented by a grid or optimization when a global
supremum is required. If a comparator has \code{m} endpoints and requires \code{k}
endpoints to pass, a composite null configuration has at least \code{m - k + 1}
non-equivalent endpoints. With \code{joint = TRUE}, all endpoint subsets with
boundary counts from \code{m - k + 1} through \code{m}, and all lower/upper
direction combinations, are evaluated, while the complete decision still
requires the \code{k}-of-\code{m} rule for every comparator in the same simulated
trial. The returned joint object includes a Bonferroni simultaneous
one-sided Monte Carlo upper bound for the maximum scenario probability.
}
