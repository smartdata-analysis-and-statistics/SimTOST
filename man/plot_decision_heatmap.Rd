% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_plots.R
\name{plot_decision_heatmap}
\alias{plot_decision_heatmap}
\title{Plot simulated decision heatmaps}
\usage{
plot_decision_heatmap(x, display = "all", ...)
}
\arguments{
\item{x}{A \code{simss}, \code{simpower}, or \code{simpower_curve} object.}

\item{display}{Comparator names to display, or \code{"all"}.}

\item{...}{Unused additional arguments.}
}
\value{
A \code{ggplot} object.
}
\description{
Shows the equivalence decision (pass or fail) for every endpoint and
comparator in every simulated trial. Each tile is one binary decision;
green means that the criterion passed and orange means that it failed. The
\code{Total} row is the combined decision for the comparator, and an
\verb{All comparators} panel, when present, shows the overall decision. This
makes isolated failures, unstable endpoints, and systematic comparator
differences easy to identify.
}
\details{
The x-axis is the simulation-trial number and has no scientific
meaning beyond showing the sequence of simulated datasets. Endpoint rows
show individual decisions; \code{Total} shows the combined endpoint decision
according to \code{k} and the specified decision rule. The heatmap is a
diagnostic of trial-level decisions, not a replacement for the numerical
power estimate.
}
