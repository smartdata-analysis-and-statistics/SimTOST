% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_joint.R
\name{sampleSize_count_joint}
\alias{sampleSize_count_joint}
\title{Estimate sample size for joint correlated count equivalence}
\usage{
sampleSize_count_joint(
  power = 0.8,
  rates,
  comparisons,
  exposure = 1,
  margin_lower = 0.8,
  margin_upper = 1.25,
  model = c("poisson", "negative-binomial"),
  dispersion = 0.1,
  alpha = 0.05,
  endpoint_corr = NULL,
  k = NULL,
  type_y = NULL,
  adjust = c("none", "bonferroni", "sidak", "t", "pc", "partial-conjunction",
    "partial_conjunction", "sequential"),
  nsim = 5000,
  seed = NULL,
  lower = 2,
  upper = 500,
  design = c("parallel"),
  list_margin_lower = NULL,
  list_margin_upper = NULL,
  optimization_method = c("fast", "step-by-step"),
  step.power = 6,
  step.up = TRUE,
  pos.side = FALSE,
  maxiter = 1000,
  ncores = 1,
  .warn_redundant_bon = TRUE
)
}
\arguments{
\item{power}{Target joint power.}

\item{type_y}{Numeric endpoint hierarchy used with \code{adjust = "seq"}: \code{1}
for primary/co-primary endpoints and \code{2} for secondary endpoints.}

\item{lower}{Minimum subjects per arm.}

\item{upper}{Maximum subjects per arm.}

\item{optimization_method}{Search method: \code{"fast"} uses bracketing and
integer bisection; \code{"step-by-step"} evaluates every candidate.}

\item{step.power}{Initial power-of-two jump for the fast search.}

\item{step.up}{Direction of the initial fast-search bracketing.}

\item{pos.side}{Retained for compatibility with \code{sampleSize()}; count
searches always return the smallest candidate reaching the target.}

\item{maxiter}{Maximum number of power evaluations.}

\item{ncores}{Number of worker processes used for count simulations.
Set to 1 for serial execution. Parallel execution splits \code{nsim} into
reproducible independent chunks and combines the resulting successes.}
}
\value{
An object of class \code{countss} containing the selected sample
size, achieved joint power, confidence interval, input parameters, and the
search history in \code{table.iter} and \code{table.test}. For count outcomes,
\code{table.iter} has one row per evaluated candidate sample size and
\code{table.test} contains complete-trial, comparator, and endpoint decision
indicators for each simulated trial and candidate. The count kernel returns
aggregate decision counts rather than raw endpoint-level test statistics, so
component columns preserve the simulated marginal success counts.
}
\description{
Estimate sample size for joint correlated count equivalence
}
