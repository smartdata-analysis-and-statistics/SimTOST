% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{simPower}
\alias{simPower}
\title{Estimate power at a fixed sample size}
\usage{
simPower(
  n,
  distribution = c("norm", "lnorm", "pois", "nbinom"),
  mu_list = NULL,
  varcov_list = NA,
  sigma_list = NA,
  cor_mat = NA,
  sigmaB = 0,
  rate_list = NULL,
  exposure = 1,
  dispersion = 0.1,
  Eper = c(0, 0),
  Eco = c(0, 0),
  rho = 0,
  TAR = NULL,
  arm_names = NA,
  ynames_list = NA,
  type_y = NA,
  list_comparator = NA,
  list_y_comparator = NA,
  alpha = 0.05,
  lequi.tol = NA,
  uequi.tol = NA,
  list_lequi.tol = NA,
  list_uequi.tol = NA,
  dtype = "parallel",
  ctype = "ROM",
  vareq = TRUE,
  k = NA,
  adjust = "no",
  dropout = NA,
  nsim = 5000,
  seed = 1234,
  ncores = 1,
  keep_sim_data = FALSE,
  .warn_redundant_bon = TRUE
)
}
\arguments{
\item{n}{Integer sample size or vector of sample sizes used for the
simulation. For parallel studies this is the base sample size used to
derive arm sizes; for 2x2 studies it is the number per sequence. A vector
returns a simpower_curve object and enables plotting power across base
sample sizes.}

\item{distribution}{Outcome distribution using R's names: \code{"norm"},
\code{"lnorm"}, \code{"pois"}, or \code{"nbinom"} (case-insensitive). Longer labels
such as \code{"normal"}, \code{"lognormal"}, and \code{"poisson"} are also accepted.}

\item{mu_list}{Named list of continuous-outcome means per arm.}

\item{varcov_list}{Optional list of covariance matrices for continuous
outcomes.}

\item{sigma_list}{Optional list of standard-deviation vectors for
continuous outcomes.}

\item{cor_mat}{Optional endpoint correlation matrix. For count outcomes,
this is also the endpoint correlation matrix used by the joint count engine.}

\item{sigmaB}{Between-subject parameter for a 2x2 design.}

\item{rate_list}{Named arm-rate list for count outcomes.}

\item{exposure}{Count exposure per subject. This can be a scalar or
endpoint vector shared by arms, or a named list of arm-specific values.}

\item{dispersion}{Negative-binomial dispersion. The per-subject
negative-binomial size is \code{1 / dispersion}; parallel-arm totals use size
\code{n / dispersion}. This can be a scalar or endpoint vector shared by arms,
or a named list of arm-specific values.}

\item{Eper}{Period effects for a 2x2 design.}

\item{Eco}{Carry-over effects for a 2x2 design.}

\item{rho}{Common endpoint correlation when \code{cor_mat} is not supplied.}

\item{TAR}{Treatment allocation rates for continuous parallel designs.}

\item{arm_names}{Optional arm names.}

\item{ynames_list}{Optional endpoint names by arm.}

\item{type_y}{Endpoint hierarchy for sequential testing for continuous and
count outcomes. Use \code{1} for primary/co-primary and \code{2} for secondary
endpoints.}

\item{list_comparator}{Named list of treatment-reference comparisons. Each
element must be \code{c(test, reference)}; the first arm is the test arm and
the second arm is the reference arm.}

\item{list_y_comparator}{Endpoint selections by comparison.
For count outcomes, the selected endpoints are used to define the count
multiplicity and the effective \code{k}; in a joint count analysis all
comparisons must currently use the same selected endpoint set.}

\item{alpha}{One-sided significance level.}

\item{lequi.tol}{Common lower equivalence bound.}

\item{uequi.tol}{Common upper equivalence bound.}

\item{list_lequi.tol}{Comparator-specific lower bounds.}

\item{list_uequi.tol}{Comparator-specific upper bounds.}

\item{dtype}{Trial design: \code{"parallel"} or \code{"2x2"}.}

\item{ctype}{Test type. Use \code{"DOM"} (difference of means) or \code{"ROM"}
(ratio of means) for Normal and Lognormal outcomes, and \code{"RR"}
(event-rate ratio) for Poisson and Negative Binomial outcomes.}

\item{vareq}{Whether variances are assumed equal for continuous outcomes.}

\item{k}{Number of endpoints required per comparison.}

\item{adjust}{Multiplicity adjustment: \code{"no"} (none), \code{"bon"}
(Bonferroni across all selected endpoints), \code{"sid"} (Sidak), \code{"k"}
(the existing weak K-fold rule), \code{"t"} (Mielke's strong
k-out-of-m adjustment, using \code{alpha / (m - k + 1)}; legacy \code{"pc"}
aliases are accepted), or
\code{"seq"} (sequential hierarchy).}

\item{dropout}{Dropout proportions. For count 2x2 studies, supply two
sequence-specific values.}

\item{nsim}{Number of simulated trials.}

\item{seed}{Random seed.}

\item{ncores}{Number of computation cores. For continuous outcomes this is
passed to the compiled simulation backend; for count outcomes it splits
Monte Carlo trials into independent seeded chunks whose C++ results are
combined.}

\item{keep_sim_data}{Logical. If \code{TRUE}, retain model-scale observations
for every simulated trial in \code{sim_data} for distribution diagnostics.
Defaults to \code{FALSE} because retained data can be large.}

\item{.warn_redundant_bon}{Logical. If \code{TRUE}, warn when a requested
multiplicity adjustment is redundant or uncalibrated.}
}
\value{
An object containing estimated power and its 95\% Monte Carlo
confidence interval. The unified function returns primary class \code{simpower}
for every distribution. If \code{keep_sim_data = TRUE}, the object also contains
long-format model-scale observations in \code{sim_data}. Count results additionally inherit from the
compatibility class \code{countpower}.
}
\description{
Calculates simulated power for a prespecified sample size.
The outcome family is selected through \code{distribution}, matching the
unified \code{sampleSize()} interface.
}
\details{
For Normal and Log Normal outcomes, supply the continuous-outcome inputs
(\code{mu_list}, \code{sigma_list} or \code{varcov_list}, and optionally \code{cor_mat} or
\code{rho}). For Poisson and Negative Binomial outcomes, supply \code{rate_list},
\code{list_comparator}, \code{list_lequi.tol}, and \code{list_uequi.tol}. Count \code{exposure}
and \code{dispersion} may be scalar, endpoint-specific, or named arm-specific
lists. Arguments for the other outcome family are not used.
The returned object supports \code{summary()}, \code{confint()}, and \code{plot()}.
The effective endpoint count is determined separately for each comparator
from \code{list_y_comparator} (or from the endpoints common to both arms when the
argument is omitted). \code{k} is checked against that count. The function warns
when an endpoint-wise adjustment is unnecessary because all selected
endpoints are required, and when \code{adjust = "no"} is used for a \code{k < m}
decision. \code{type_y} is used with \code{adjust = "seq"} for both continuous and
count outcomes; for other adjustments it is ignored with a warning.
For a \code{k}-of-\code{m} decision, \code{adjust = "t"} allocates alpha over the
\code{m - k + 1} boundary endpoints relevant to the strong k-out-of-m null.
The legacy \code{adjust = "pc"} label is accepted as an alias.
Comparisons always use the order supplied in \code{list_comparator}: the first
arm is the test and the second arm is the reference. Thus, \code{c("T", "R")}
gives \code{T - R} for DOM, \code{T / R} for ROM, and the event-rate ratio
\code{rate_T / rate_R} for RR. Reversing the two names reverses the estimand.
For count outcomes, the estimand is the event-rate ratio \code{lambda_T / lambda_R}.
The equivalence hypotheses are \code{H0: lambda_T / lambda_R <= L} or
\code{lambda_T / lambda_R >= U} versus \verb{H1: L < lambda_T / lambda_R < U},
assessed by TOST. The same interval hypotheses apply to continuous mean
differences (DOM) or mean ratios (ROM), with the log-normal ROM analysis
performed on the log scale.
}
\examples{
simPower(n = 100, distribution = "Poisson",
      rate_list = list(TEST = .21, REF = .20),
      list_comparator = list(TEST_vs_REF = c("TEST", "REF")),
      list_lequi.tol = list(TEST_vs_REF = .80),
      list_uequi.tol = list(TEST_vs_REF = 1.25),
      exposure = 10, nsim = 100, seed = 1)
}
